module.exports = {

first: function(array) {
    results = array[0];
    return results;
  },

last: function(array) {
  for (var i = 0; i < array.length; i++) {
    results =  array[i] - 1;
  }
  return results
}












}
